import test from 'node:test';
import assert from 'node:assert/strict';
import {applyAction} from '../lib/simulator/engine.mjs';
import {pool,cards,state,unit,inst,fund,attack} from './fixtures/simulator-audit.mjs';
for(const number of ['hBP02-041','hBP05-043','hSD03-006','hSD03-009'])test('Okayu mascot native payment '+number,()=>{const card=cards.find(c=>c.number===number);const artIndex=card.arts.findIndex(a=>/(應援|加油|吶喊).*(存檔|檔案)/.test(a.effect));let s=state(number);fund(s.players[0].zones.center,card.arts[artIndex].cost);s.players[0].zones.center.attachments=[inst('hSD03-013','mascot')];s.players[1].zones.back1=unit('AUDIT-DUMMY');const before=s.players[0].zones.center.cheer.length;s=applyAction(s,0,{...attack,artIndex},pool,()=>0);if(number==='hSD03-009')s=applyAction(s,0,{type:'choose',cheerId:'cheer0'},pool,()=>0);assert.ok(s.pendingChoice.cheerOptions.some(c=>c.id==='mascot'));s=applyAction(s,0,{type:'choose',cheerId:'mascot'},pool,()=>0);while(s.pendingChoice)s=applyAction(s,s.pendingChoice.playerIndex,{type:'choose',zone:'back1'},pool,()=>0);assert.equal(s.players[0].zones.center.attachments.length,0);assert.equal(s.players[0].zones.center.cheer.length,before-(number==='hSD03-009'?1:0));assert.equal(s.players[0].turnEvents.cheerArchived,number==='hSD03-009'?1:0);assert.ok(s.players[1].zones.back1.damage>0);});
