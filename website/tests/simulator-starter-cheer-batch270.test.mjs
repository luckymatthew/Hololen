import test from 'node:test';
import assert from 'node:assert/strict';
import {applyAction} from '../lib/simulator/engine.mjs';
import {cards,pool,state,inst,unit} from './fixtures/simulator-audit.mjs';
for(const [firstPlayer,turnsTaken,valid] of [[1,1,true],[0,1,false],[1,2,false]])test('Pekora cheer timing '+firstPlayer+'/'+turnsTaken,()=>{let s=state('hSD09-006');s.phase='main';s.firstPlayer=firstPlayer;s.players[0].turnsTaken=turnsTaken;s.players[0].zones.back1=unit('hSD09-006');s.players[0].zones.back2=unit('AUDIT-DUMMY');s.players[0].cheerDeck=[inst('hY01-001','top'),inst('hY02-001','bottom')];s=applyAction(s,0,{type:'collab',zone:'back1'},pool,()=>0);if(valid){assert.equal(s.pendingChoice.optional,false);assert.ok(!s.pendingChoice.options.includes('back2'));s=applyAction(s,0,{type:'choose',zone:'center'},pool,()=>0);assert.equal(s.players[0].zones.center.cheer[0].id,'top');assert.equal(s.players[0].cheerDeck[0].id,'bottom');}else{assert.equal(s.pendingChoice,null);assert.equal(s.players[0].cheerDeck.length,2);}});
test('Watame archive cheer targets only fourth-generation 2nd',()=>{const target=cards.find(c=>c.stage==='2nd'&&c.tags?.includes('#4期生'));let s=state(target.number);s.phase='main';s.players[0].zones.back1=unit('hSD08-007');s.players[0].archive=[inst('hY01-001','cheer')];s=applyAction(s,0,{type:'collab',zone:'back1'},pool,()=>0);assert.equal(s.pendingChoice.optional,true);s=applyAction(s,0,{type:'choose',cardIds:['cheer']},pool,()=>0);assert.deepEqual(s.pendingChoice.options,['center']);s=applyAction(s,0,{type:'choose',zone:'center'},pool,()=>0);assert.equal(s.players[0].zones.center.cheer[0].id,'cheer');});
